name: Github Actions - Workflow Dispatch Templates
run-name: Running - ${{ github.event_name }}
on: [workflow_dispatch]

jobs:
  on-workflow_dispatch:
    runs-on: ubuntu-lateste
    steps:
      - name: Comentário
        run: echo "Comentário WORKFLOW DISPATCH - Básico"
